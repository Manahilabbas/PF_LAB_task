#include <stdio.h>
int main()
{
int number;
printf ("enter a number");
scanf("%d",&number);
if (number%3==0)
  printf(" number is multiple of 3");
else
 printf("numebr is not multiple of 3");
}
